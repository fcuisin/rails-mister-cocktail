<%= simple_form_for([@cocktail, @dose]) do |f| %>

    <%= f.association :ingredient, collection: Ingredient.active.order(:name), prompt: "Choose an ingredient" %>
    <%= f.input :description %>
    <%= f.button :submit %>

<% end %>

<%= link_to "Back", cocktail_path(@cocktail) %>
